import React from "react";
import { NavLink, Routes, Route } from "react-router-dom";

const App = () => {
  return (
    <div>
      <Routes>
        <Route path="url" element={<컴포명 />} />
      </Routes>
    </div>
  );
};

export default App;
